apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/server-snippet: |
      location ~ /static/ {
        expires 1M;
        add_header Cache-Control "public";
      }
  labels:
    app.kubernetes.io/instance: frontend
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: productlegislationnavigator
    app.kubernetes.io/version: 1.0.1
    helm.sh/chart: react-1.0.0
  name: productlegislationnavigator-overwrite
  namespace: productlegislationnavigator
spec:
  rules:
  - host: overwrite.productlegislationnavigator.devcfs.com
    http:
      paths:
        - pathType: ImplementationSpecific
          backend:
            service:
              name: frontend-productlegislationnavigator
              port:
                number: 80
  tls:
  - hosts:
    - overwrite.productlegislationnavigator.devcfs.com
    secretName: tls-certs
