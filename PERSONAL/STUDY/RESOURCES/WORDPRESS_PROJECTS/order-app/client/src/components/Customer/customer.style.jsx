import styled from 'styled-components'
import { Link } from 'react-router-dom'

export const CustomerContent = styled(Link)`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  padding: 0.5rem 0;
  flex: 1;
`

export const Info = styled.p``
